apply plugin: 'java'

repositories {
    mavenCentral()
}

// Needed to be used with android projects
sourceCompatibility = 1.7
targetCompatibility = 1.7


dependencies {
    testCompile 'junit:junit:4.12'
    compile 'org.json:json:20160212'
}